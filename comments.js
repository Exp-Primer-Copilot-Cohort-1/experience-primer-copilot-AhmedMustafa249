// create a web server
const express = require("express");
const app = express();
const port = 3000;

// load the comments module
const comments = require("./comments");

// load the comments module
const comments = require("./comments");

// create a new comment
comments.create("I love this blog post!");

// retrieve all comments
const allComments = comments.getAll();

// log all comments to the console
console.log(allComments);

app.listen(port, () => {
  console.log(`Server is listening on http://localhost:${port}`);
});
// Path: comments.js
// create a web server
const express = require("express");
const app = express();
const port = 3000;

// load the comments module
const comments = require("./comments");

// create a new comment
comments.create("I love this blog post!");

// retrieve all comments
const allComments = comments.getAll();

// log all comments to the console
console.log(allComments);

app.listen(port, () => {
  console.log(`Server is listening on http://localhost:${port}`);
});
// Path: comments.js
// create a web server
const express = require("express");
const app = express();
const port = 3000;

// load the comments module
const comments = require("./comments");

// create a new comment
comments.create("I love this blog post!");

// retrieve all comments
const allComments = comments.getAll();

// log all comments to the console
console.log(allComments);

app.listen(port, () => {
  console.log(`Server is listening on http://localhost:${port}`);
});
// Path: comments.js
// create a web server
const express = require("express");
const app = express();
const port = 3000;

// load the comments module
const comments = require("./comments");

// create a new comment
comments.create("I love this blog post!");

// retrieve all comments
const allComments = comments.getAll();

// log all comments to the console
console.log(allComments);

app.listen(port, () => {
  console.log(`Server is listening on http://localhost:${port}`);
});
// Path: comments.js
// create a web server
const express = require("express");
const app = express();
const port = 3000;

// load the comments module
const comments = require("./comments");

// create a new comment
comments.create("I love this blog post!");

// retrieve all comments
const allComments = comments.getAll();

// log all